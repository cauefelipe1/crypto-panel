import { Outlet } from "react-router-dom";

export default function Dashboard() {
  return (
    <div>
      <div>
        <p>Personal crypto panel</p>

        
      </div>

      <Outlet/>
    </div>
  );
  }